# Backend environment example (do NOT commit your real .env)
# Copy to .env and fill values:
#   cp backend/.env.example backend/.env

# Required for /api/ai/beach-report
OPENAI_API_KEY=

# Optional: model used by backend/app/api/ai.py
OPENAI_MODEL=gpt-4o-mini

# Optional: where to store form submissions (SQLite)
# For a VPS/Docker deploy, mount this path as a persistent volume.
DB_PATH=data/app.db

# Forms storage backend: sqlite (default) or firestore (Cloud Run recommended)
FORMS_STORAGE=sqlite

# Firestore project (optional; Cloud Run sets GOOGLE_CLOUD_PROJECT automatically)
FIRESTORE_PROJECT=sahiller-bizimle-temiz-481410

# Optional toggles (already supported by the backend)
# PREWARM_ENABLED=1
# PREWARM_DAYS=7
# FILL_GAPS_ENABLED=0
# IMPUTE_MISSING_ENABLED=1
# IMPUTE_LOOKBACK_DAYS=5
