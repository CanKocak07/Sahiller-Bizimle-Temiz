# Backend environment example (do NOT commit your real .env)
# Copy to .env and fill values:
#   cp backend/.env.example backend/.env

# Required for /api/ai/beach-report
OPENAI_API_KEY=

# Optional: model used by backend/app/api/ai.py
OPENAI_MODEL=gpt-4o-mini

# Optional toggles (already supported by the backend)
# PREWARM_ENABLED=1
# PREWARM_DAYS=7
# FILL_GAPS_ENABLED=0
# IMPUTE_MISSING_ENABLED=1
# IMPUTE_LOOKBACK_DAYS=5
